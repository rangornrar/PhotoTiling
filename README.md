# PhotoTiling
This program provides a Tkinter GUI to split a mosaic image into a configurable grid. Users can preview the image with an overlayed grid, adjust rows, columns, and trim margins, then export each tile as separate image files in PNG, JPG, or WebP format.
